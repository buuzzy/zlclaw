# 桌面端 sage-api 的默认环境变量模板。
#
# 本文件 **仅作为示例**，不会进入 .app 包。
# CI 构建（.github/workflows/release.yml）在 tauri build 之前
# 会用 GitHub Secrets 生成同目录下的真正 `.env`，再被打进 .app/Contents/Resources/。
#
# 严格规定：
#   - 永远不要把 SUPABASE_SERVICE_ROLE_KEY、签名私钥等 god-mode 凭证
#     写入打包的 .env。这些应当只保留在受控服务端（如 Railway）。
#   - 桌面端只放公共可暴露的字段：URL、anon key、可选的金融数据 API key。
#     - SUPABASE_URL：公开 endpoint
#     - SUPABASE_ANON_KEY：受 RLS 保护，公开合规
#     - IWENCAI_API_KEY / WESTOCK_API_KEY：当前选择跟前端一起分发；
#       未来若要改 BFF 中转，可以从此移除并在 BFF 注入。

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# 可选：金融数据 API key（用户可在 ~/.sage/.env 里覆盖成自己的）
# IWENCAI_API_KEY=
# WESTOCK_API_KEY=
