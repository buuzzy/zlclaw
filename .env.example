# Sage 前端环境变量示例
# 复制为 .env.development (本地开发) 或 .env.production (打 release 包) 后填入真实值
# .env.* 已在 .gitignore，不会提交到仓库

# ─── Supabase ───────────────────────────────────────────────────────────────
# 每个 Supabase project 在 Dashboard > Project Settings > API 页面查看
# VITE_SUPABASE_URL      — Project URL（形如 https://xxxxxxxx.supabase.co）
# VITE_SUPABASE_ANON_KEY — anon / public key（公开 key，由 RLS 限制，可放前端）
#
# 未配置时，代码会 fallback 到 prod 默认值（src/shared/lib/supabase.ts 中硬编码）。
# 强烈建议：
#   - .env.development 填 sage-dev project（本地开发不污染真实用户数据）
#   - .env.production 填 sage（内测 / 发布）project
#
# VITE_SUPABASE_URL=https://xxxxxxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGci...

# ─── API sidecar（与当前 HT Claw 历史兼容，保留）─────────────────────────────
# iwencai Open API Key（金融数据）
# IWENCAI_API_KEY=sk-proj-...

# 渠道 API Key（微信/飞书等外部渠道认证，留空则免认证）
# HTCLAW_CHANNEL_API_KEY=
